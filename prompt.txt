Siemens TIA portal 18: I export DBs from the TIA portal 18 project to the VS code project folder. Need to cross-check those DBs default value settings for the insteunet instances.
for the system family Brew follow the SysConfig_Lib_Brew.db
the souece of true is comeing from hardware specification Excel testLBB.xlsx
where the rows are different tab label and column is indicating system family like BREW350, BREW 450 etc.
you should see the DB comments and should discover the MachineConfig[7] is for Brew 350.

we will looking for the bool tag setting on the DB for respective instrument what is given at xlsx.
ENABLE  : Bool;
EXIST   : Bool;

you need to read thorough the full DB structure and discover e.g. brows the MachineConfig[7]>Unit Process or any > every EM-XXX (like EM-201)>....> finally the ENABLE and EXIST tag.
   
you should read the general default value also somewhere you found in DB.
If tag name is not available or not bound in excel or in DB use the default setting 

ENABLE  : False;
EXIST   : False;

IF any tagLabel has option description on excel like BLENDING, RECIRCULATION, COOLING RECIRCULATION, SRU, FEED PUMP
and the value marked 'o' 
then, 
	ENABLE = false and  EXIST = false;

otherwise,
Consider excel tag reading rules, 
IF  marked 'X' 
	ENABLE = true and  EXIST = true;
if marked 'o' 
	ENABLE = true and  EXIST = false;
if marked enpty or sapre 
	ENABLE = false and  EXIST = false;

for AV valve,
VLV_W_FB, FB OPN, FB CLS Logic (AV valves only)
- FB OPN = `x` or `o` → FB_OPN_EN = True and VLV_W_FB = True
- FB CLS = `x` or `o` → FB_CLS_EN = True and VLV_W_FB = True
- FB OPN AND FB CLS = spare or empty → VLV_W_FB = False

### Option Module Rule (based on Function column)
If a tag is found under **`Options."EM - XXX"`** in the DB (unit = `"Options"` in auto-discovered tag map):
(if tag **is** option-type)
- ENABLE = False,  EXIST = False  — unconditionally, regardless of Excel value.


### NO tag logic for CW structure of CW_AN, CW_VLV
if exel Type column value = 'NC'
	tag NO = False
if exel Type column value = NO
	tag NO = True

If excel has commens on any cell, the like tagLabel PCV22X-1 (read the comments for the value of X, this might be 0,1 ,2 or any thing what mention on the comments) 
	then map the value what is given on BREW350 column according to the comments of the cell. 
		e.g PCV22X-1 ( here cell comments X is 0 or 1) the corronponding column value (X with comments PCV220-1, means ENABLE=EXIST=true for PCV220-1 for BREW350)
	
if any attibure of instrumetn AV FCV or any undet the EM struct in the DB file is empty like () then consifer it as False (default). if attibure is written as True then only consider it True. 
e.g. AV208-1" { ExternalAccessible := 'False'; ExternalVisible := 'False'; ExternalWritable := 'False'} : "STAT VLV" := ('AV208-1', ((), (), (), (), (), (), TRUE, TRUE, (), (), (), (), (), (), (), TRUE), (), 182, 183, 161); 
here for the AV208-1 () filed is false for bool. and is written True as required.


We want to read all the DB and check this two value mismatch . Example first compare for BREW350(fanily name) and Brew 450
the hardware specification excel showing the family name (BREW350 or etc) and marked 'X' or 'o' or empty.

Finally create conplete the excel with all compararison. produce an updated SysConfig_Lib_Brew.db that i will import on the TIA portal project directly to run the project with out any other issue.
	
		
now read the promt and by deep analysis update the wishList.md and make a clean DesignStructure.md plan the excicute the plan and do the assignment. 

updates:
the running TAG_MAP is done for SysConfig_Lib_Brew.db. 
Read the new SysConfig_Lib_Clara.db, SysConfig_Lib_CR.db, SysConfig_Lib_KR.db and SysConfig_Lib_PP.db in the project folder. 
Every .db has some similara and few different EMs. 
And the MachineConfig [] also referefing different families. 
That is why, I want to do indipendent operation but same job for different .dbs againes the hardware specificatin Excel like testBrew.xlsx, new name testLBB.xlsx by providing the mc_idx and excel col_idx. 
So that no manual TAG_MAP needed. just refering the .bd name and hardware specifictin exel colomn info 
with machine id. can generate the comparison excel and updated db respectively. Need to generate separate conparison exel and corrected indivisual updated db. 
Now add this task in to the wishList.md then plan and update the designStructure.md and learning .md and excicure the task. 


Correction:
Tag name updated. PLC has full form. 
L-GREEN
L-YELLOW
L-RED
L-BUZZ
